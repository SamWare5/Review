class Cat:
    name = ""
    age = 0
    registred = False

    def __init__(self, input_name, input_age):
        self.name = input_name
        self.age = input_age

        if input_age>2:
            self.registred=True

        def meow(self, number_times):
            for m in range(number_times):
                print("Meow")
        def meow(self, number_times, phrase""):
            for m in range(number_times):
                print("Meow" + phrase)

c1= Cat("Jack", 2)
c2 = Cat("Samatha", 9)

print("Cat c1 registred")
